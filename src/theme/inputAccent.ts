import { colors } from './colors';

/** Shared TextInput accent (olive green, no browser default orange). */
export const inputAccent = {
  selectionColor: colors.primary,
  cursorColor: colors.primary,
} as const;
